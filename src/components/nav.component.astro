<header>
  <nav
    class="flex flex-col items-center justify-between p-4 h-screen shadow-xl gap-4"
  >
    <div>Logo</div>
    <div class="flex flex-col w-full">
      <button><i class="fi fi-rr-star text-yellow"></i>New</button>
      <button><i class="fi fi-rr-fire-flame-curved text-red"></i>Popular</button
      >
      <button><i class="fi fi-rr-shuffle text-blue"></i>Random</button>
    </div>
    <div class="flex items-center border border-dark px-2 rounded-md w-40">
      <i class="fi fi-rr-search"></i>
      <input
        class="w-full h-full py-2 bg-transparent outline-none"
        type="text"
        placeholder="Search..."
      />
    </div>
  </nav>
</header>

<style>
  button {
    @apply p-2 rounded-md text-lg text-start;
  }
  button:hover {
    @apply bg-dark text-light;
  }
  i {
    @apply mr-2;
  }
</style>
